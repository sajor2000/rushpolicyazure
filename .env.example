# ============================================================================
# Rush Policy Chat Assistant - Environment Configuration
# ============================================================================
#
# This file contains example environment variables for the application.
# Copy this file to .env.local and fill in your actual values.
#
# IMPORTANT: Never commit .env.local to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# Azure AI Agent Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# These variables configure the Azure AI Projects agent connection.
# The agent uses Azure GPT-5 Chat Model for policy queries.

# Azure AI Projects endpoint (includes project path)
AZURE_AI_ENDPOINT=https://rua-nonprod-ai-innovation.services.ai.azure.com/api/projects/rua-nonprod-ai-innovation-project

# Azure AI Agent ID (Policy_Tech_V1)
AZURE_AI_AGENT_ID=asst_301EhwakRXWsOCgGQt276WiU

# ----------------------------------------------------------------------------
# Azure Authentication (OPTIONAL for local dev, REQUIRED for some deployments)
# ----------------------------------------------------------------------------
# DefaultAzureCredential tries authentication in this order:
#   1. Environment variables (these settings)
#   2. Managed Identity (Azure App Service, Container Instances)
#   3. Azure CLI (`az login`)
#   4. VS Code Azure extension
#
# For local development: Use `az login` instead of setting these variables
# For Azure App Service: Use Managed Identity (recommended) or set these
# For other platforms: Set these environment variables

# AZURE_CLIENT_ID=your-service-principal-client-id
# AZURE_CLIENT_SECRET=your-service-principal-client-secret
# AZURE_TENANT_ID=your-azure-tenant-id

# ----------------------------------------------------------------------------
# Production Configuration
# ----------------------------------------------------------------------------

# Node environment (development | production)
# NODE_ENV=production

# CORS allowed origin for production (restrict API access)
# ALLOWED_ORIGIN=https://your-app-name.azurewebsites.net

# ----------------------------------------------------------------------------
# Optional: Application Insights (Azure Monitoring)
# ----------------------------------------------------------------------------
# Enable Application Insights for production monitoring and logging

# APPINSIGHTS_INSTRUMENTATIONKEY=your-instrumentation-key

# ============================================================================
# Notes:
# ============================================================================
#
# 1. The application uses Azure AI Agent (NOT Azure OpenAI Assistants API)
# 2. Authentication uses DefaultAzureCredential for maximum flexibility
# 3. For production on Azure: Enable Managed Identity instead of credentials
# 4. Thread management is STATELESS (fresh thread per request for RAG accuracy)
# 5. Rate limiting: 20 requests per minute per IP address
#
# Troubleshooting:
# - If auth fails locally: Run `az login` in terminal
# - If auth fails on Azure: Enable Managed Identity and assign permissions
# - Check logs for detailed error messages with request IDs
#
# Documentation: See docs/AZURE_DEPLOYMENT.md for deployment instructions
# ============================================================================
